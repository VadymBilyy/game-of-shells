import React from 'react';
import ReactDOM from 'react-dom';
import { CupsContainer } from './components/cups-container/cups-container';

ReactDOM.render(
	<React.StrictMode>
		<CupsContainer />
	</React.StrictMode>,
	document.getElementById('root'),
);
